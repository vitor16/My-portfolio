version: "3.8"

services:
  portfolio:
    image: nginx:alpine
    container_name: liquid-portfolio
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    environment:
      - TZ=America/Sao_Paulo
